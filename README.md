# homebrew-releases

Homebrew tap for Autopilot and related tools.

The `autopilot` formula installs the public OSS CLI from
`vkorir/autopilot` releases. Private cloud/control-plane tools should use
separate private formulas or private package/container artifacts.
